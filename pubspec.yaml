name: flutter_movie_list
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_serializable: ^4.0.2
  get: ^3.26.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

